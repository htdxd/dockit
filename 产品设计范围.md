# AI 文档产物工具箱：产品设计范围

## 1. 产品定位

本产品是一个极轻量、即开即用的 AI 文档产物工具箱。

它将 Coding Agent 中经过验证的 Skill 能力封装为面向普通用户的小工具：用户选择一个日常任务、提供必要的材料与要求，软件在本地受控环境中调用用户自行配置的 LLM，最终输出可由专业软件继续编辑的文件。

产品交付的是“可用产物”，而不是通用 AI 对话能力。

## 2. 核心承诺

- 打开速度快，界面和操作步骤极少。
- 用户不需要安装、理解或操作 Claude Code、Codex 等 Agent 产品。
- 用户可填写 `base_url`、`api_key` 与模型名，自由接入 OpenAI、Anthropic、OpenAI-compatible 网关或本地模型。
- 所有任务都有清晰的输入、执行过程和文件产物。
- 产物保存到用户选择的本地目录，可用 Word、WPS、PowerPoint、Adobe 等专业软件继续使用。

## 3. V1 工具范围

首批仅提供官方维护的高频文档任务：

1. PPT 生成：主题、材料与风格要求 → `.pptx`。
2. 简历生成：个人经历、项目、目标岗位与材料 → `.docx` / `.pdf`。
3. DOCX 生成：结构化要求与参考材料 → `.docx`。
4. PDF 转 DOCX：PDF 文件 → 可编辑 `.docx`，必要时进行版式、表格或扫描件修复。

### 3.1 已确认的 V1 工具深度

- **PPT 生成**：使用已接入的 `ppt-master` Skill，保留其材料分析、SVG 页面生成、质量检查与 PPTX 转换工作流。`ppt-master` 的发布依赖固定与仓库打包在 Release 阶段处理，不改变 V1 功能范围。
- **简历生成**：内置 5 套精选模板，支持照片替换、模板文本框原位填充、内容溢出估算以及渲染检查，尽量保留模板原有字体与版式。
- **DOCX 生成**：完整保留 `simple`、`standard`、`academic`、`gongwen`、`form`、`template` 六种复杂度模式，覆盖普通文档、标准报告、学术文档、公文、可填写表单和模板套用。
- **PDF 转 DOCX**：保留 PDF 类型判断、MinerU 转换、机械审计以及具备 Vision 能力时的渲染检查。

后续是否新增工具，不取决于“是否能写成一个 Skill”，而取决于是否同时满足以下条件：

- 是高频、日常且明确的用户任务；
- 输入与输出文件类型清晰；
- 用户拿到产物后可交给现有专业软件完成最后编辑；
- 能在受控工具权限内稳定执行并验证质量。

## 4. 明确不做的事

V1 不做以下功能：

- 全局或无任务目标的通用 Chatbot。
- 文档、PPT、PDF 的内置预览器或编辑器。
- 面向普通用户的任意 Skill 安装、插件市场或工作流编排。
- 可自由操作用户整台电脑的通用桌面 Agent。
- 面向普通用户的任意 MCP Server 配置与连接。

这些限制不是能力缺失，而是为了维持“轻量、稳定、即开即用”的产品承诺。

## 5. 交互模型：任务内受控对话

复杂任务（尤其是简历和 PPT）需要 LLM 澄清信息，也需要用户根据实际产物提出修改意见。因此产品需要对话能力，但不需要独立的聊天页面。

每个任务页只出现以下三类交互：

1. **任务进度**：展示有限且可理解的执行状态，例如“正在提取 PDF 内容”“正在生成第 3 页 PPT”。
2. **澄清问题**：当缺少会明显影响产物质量的信息时，LLM 发出结构化问题。界面使用弹窗或页面内卡片呈现多字段表单，可包含单选、多选、短文本、长文本、日期和文件补充。
3. **结果反馈**：用户在外部专业软件查看产物后，可回到当前任务输入修改要求，例如“压缩到一页，突出 Python 和深度学习”。系统基于当前版本生成新文件，而不是覆盖旧文件。

澄清问题应遵循以下规则：

- 表单优先；已知的固定信息不通过自然语言来回询问。
- 仅追问高影响、无法从材料可靠推断的信息。
- 限制追问轮数；用户可选择“跳过，按当前资料生成”。
- 对话上下文仅限当前工具、当前任务和当前产物版本。

因此，用户体验是“完成一个文件任务的受控闭环”，而不是使用另一个通用聊天客户端。

## 6. 版本化产物

每次生成或修改都保留独立产物版本，例如：

```text
resume-v1.docx
resume-v2.docx
resume-v3.docx
```

软件记录产物路径、生成时间、输入材料与本次修改要求。用户可随时在默认专业软件中打开任一版本。V1 不承担版本内容对比或文件编辑职责。

## 7. 模型接入原则

Runtime 定义自己的统一请求和工具调用协议，Skill 与工具层不直接依赖特定厂商 SDK。

- 优先支持 OpenAI-compatible 配置：`base_url`、`api_key`、`model`。
- 分别提供 OpenAI 与 Anthropic 的原生 adapter，以正确处理各自的工具调用、流式输出、JSON Schema 与错误语义。
- 用户可以保存多组 Provider 配置，并选择其中一组作为当前任务的活动配置。
- 支持对 `vision`、`tool_calling` 进行显式能力覆盖；解析优先级为「用户覆盖 → 最近一次同指纹能力探测 → 已知模型能力表 → 安全默认值」。
- 设置页可发起真实、最小、确定性的能力探测（Tool Calling / Vision / Reasoning Control），结果为四态（verified / unsupported / probe_error / unknown），随 `kind+base_url+model` 指纹缓存；三者任一变化即失效。
- 用户可保存统一的「生成质量」档位（自动 / 快速 / 平衡 / 深入），随任务发送给 Provider；`auto` 不主动发送推理参数，模型不支持时安全省略对应参数。
- Skill 声明所需模型能力，例如 `tool_calling`、`vision`。
- 任务开始前检查模型能力；不兼容时明确报错，不在执行中静默降级。
- 能力探测与运行日志不得写出 API Key、raw CoT 或 thinking block。

用户需知晓：其提供的文本和文件材料将按所选 Provider 的协议发送给对应服务。

## 8. Runtime 与工具安全

产品内部可以采用极简 Agent tool-call loop，但工具必须受控。

基础能力包括：

- 在任务 workspace 内读取、写入和编辑文件；
- 通过受控命令执行器调用本地转换、渲染或生成工具；
- 管理输入、临时文件、日志与最终 Artifact。

命令执行器不得向模型暴露完整终端权限，应至少具备：

- 固定的任务工作目录与输出目录；
- 禁止越界路径访问；
- 危险命令、删除、覆盖、网络访问的策略控制与必要时的用户确认；
- 可审计的命令、输入和输出日志；
- 清晰的任务超时和失败恢复机制。

每个任务保留完整的本地 JSONL 调试日志，包括必要的模型回合、工具参数、工具结果与错误信息；界面仅展示经过截断的摘要。日志属于本地任务数据，不发送给额外的第三方服务。

## 9. 统一材料理解与信息复用

用户上传的材料不仅包含正文，还可能包含图片、表格、LaTeX 公式、代码段和版式关系。Runtime 应尽量保留并复用这些信息，避免把材料退化为纯文本摘要。

内部采用统一材料 IR：

- Markdown 作为可顺序阅读的主体表示，保留标题层级、表格、LaTeX 公式和 fenced code block；
- 图片与其他媒体物化为独立文件，并在 Markdown 中保留引用；
- manifest 记录来源文件、格式、转换状态、主体文档路径、媒体路径及尺寸等元数据；
- 原始文件始终保留在任务 workspace，Skill 可在统一 IR 不足时回读原件。

处理工作流为：

```text
上传材料
  → 暂存到隔离 workspace（按内容 hash 去重，sources/<id16>/）
  → 进入 Agent loop 前统一预处理（MaterialService）
  → md/txt 原生解析；PPTX 复用 ppt-master parser；PDF/DOCX 按需走 read/ingest
  → 归一化为共享 DocumentIR + 兼容 Markdown 投影 + Asset 目录
  → Runtime 向 Skill 提供材料摘要与可读取路径（[MATERIALS] 横幅）
  → Skill 先读摘要、写 ContentPlan，再按任务读取正文、图片、表格、公式或代码
  → 生成产物并执行对应质量门（机械门 + 可选视觉门）
```

小型材料可自动萃取以减少 Agent 步数；大型材料和高成本解析按需执行。解析结果在当前任务内按内容 hash 复用，同一材料只解析一次。具体 Skill 仍负责决定哪些材料信息与当前产物相关，不把全部内容无差别塞入模型上下文。

### 9.1 统一材料 IR 与双模式

- 四个功能共用同一份 DocumentIR（`work/materials/<id>/document.json`：block/asset 版本化模型，图片块保留位置引用）与 `content.md` 顺序阅读投影（由 IR 单向生成）。
- 四个 Prompt 都必须先写 `work/plans/content-plan.json`（selections/exclusions 留痕）再生成；候选资源不能静默丢弃。
- `vision=true`：Agent 可读取候选图片并做有界渲染复核；`vision=false`：保守模式（高置信度资源 + 最多一次批量提问 + 机械门），QA 明确区分「机械检查通过」与「视觉检查未执行」，不得合并成模糊的"质量检查已完成"。
- 结果页分别显示机械检查与视觉检查状态，并显示已采用/舍弃的资源数量。

## 10. 质量检查与渲染能力部分产物需要视觉或版式质量检查，但这不意味着需要让用户在软件内编辑或配置 MCP。

软件可内置受控的处理链：

```text
生成 DOCX/PPTX
  → 本地渲染为 PDF 或图片
  → 机械规则检查（结构/几何/容量/关系/文件完整性，硬门）
  → 具备 Vision 能力的 LLM 检查（语义/审美补充，不能覆盖机械硬错误）
  → 必要时有界修复（最多 3 轮）
  → 输出最终产物
```

例如，简历可检查是否超页、文字溢出或层级混乱；PPT 可检查页面布局、可读性和内容溢出。此能力属于官方任务实现的一部分，不向用户暴露为通用截图或 MCP 工具。

### 10.1 简历受限版式

- 仅对 5 套精选模板支持受约束的组件动作（替换文本 / 替换资产 / 扩高 / 纵向平移 / 组件复制），动作必须在模板 manifest 的 `allowed_actions` 白名单内。
- 模板 manifest 记录 `template_sha256`；hash 不匹配时立即失败并提示重新索引，不承诺高保真。
- 组件复制原子处理唯一 ID 与关系；含图片组件不复制；任意未索引模板不进入组件变换路径。

## 11. Skill 与 MCP 的扩展策略

产品内部应保留可扩展的 Skill manifest 和工具 adapter，以便官方持续增加能力；但 V1 不把它们当作用户功能。

未来如出现明确、稳定且安全的第三方扩展需求，可分层开放：

1. 内置 Skill：官方维护、默认安全、开箱即用。
2. 导入 Skill：具备 manifest、版本、依赖和权限声明，经用户确认后安装。
3. Developer Mode：面向开发者加载本地目录或调试 MCP adapter，并清楚标记为未验证能力。

在此之前，任意第三方 Skill/MCP 会带来外部进程管理、依赖安装、权限审计、供应链安全、跨平台兼容与支持成本，不符合 V1 的定位。

## 12. 最终产品定义

> 一个会主动补齐必要信息、可靠地产出日常文件，并允许用户针对结果进行有限反馈重做的极轻量 AI 文档工具箱。

它使用 Agent 与 Skill 技术实现，但对用户呈现为几个明确、快速、可信赖的文件任务工具。

## 13. 前端视觉基准（Mockup 指导文件）

前端界面实现以 B 方案静态原型为**唯一视觉基准与指导文件**：

- **路径**：`design-mockups/mockup-B-dashboard.html`
- **定位**：后续所有前端（Tauri + Vite 的 `src/` 页面）的布局、配色、组件样式、交互反馈（弹窗 / Toast / 进度动画）、主题切换均以此文件为准。
- **约束**：该文件的布局与视觉规范**不可随意修改**；任何调整（组件结构、配色、圆角、阴影、间距、文案位置等）都必须先征得用户确认后方可进行。
- **当前已确定的视觉规范要点**（以此文件为准，历史版本 A/C 已废弃删除）：
  - 导航：左侧栏为唯一一级导航（4 工具 + 设置），顶部不重复放置一级标签。
  - 配色：浅色模式主色深蓝 `#1e3a8a`，深色模式主色浅蓝 `#7cb3ff`；按钮无阴影、圆角偏小（卡片 8px / 按钮 7px）。
  - 上传区：四个工具的"上传参考资料"为明显的大面积虚线区域，并明确标注"可选"。
  - 任务内交互：仅三类受控交互（任务进度、澄清问题、结果反馈），配合澄清弹窗与 Toast 状态反馈。
  - 特殊输入：DOCX 文档用途含"作业报告"并可自定义；PPT 目标页数支持精确值/区间；设置页通过"获取模型列表"按钮加载可选模型。

浏览器运行模式只用于前端测试、Mockup 对照和开发期验证，不属于正式产品交付形态；Release 仅交付 Tauri 桌面应用，不承诺浏览器环境中的数据持久化或完整任务执行能力。
