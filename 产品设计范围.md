# DocKit Resume：产品设计范围

> 当前交付范围：Windows 桌面版。本文按现行界面和简历工作流描述，不沿用历史“AI 文档产物工具箱”规划。

## 1. 产品定位

DocKit Resume 是面向中文简历的 AI 制作工具。用户可以上传已有材料、手动填写经历，或同时使用两者；系统在必要时追问关键事实，按所选模板生成简历，交付可编辑 DOCX、PDF 或两者。

产品聚焦从零制作、整理已有材料、迁移到组件化模板以及按要求修改已有简历。界面围绕明确的简历任务和文件产物组织，不提供通用聊天工作台。

## 2. 当前交付与运行前提

- **交付形态**：Windows 桌面应用。用户在“设置”中配置模型供应商、模型、MinerU Token 和本地输出目录；在“简历生成”中创建任务，在“进行中”查看状态，在“产物版本”中打开文件。
- **排版环境**：高质量生成路径需要 Microsoft Word 或 WPS 文字以及 Poppler。测量文本高度、换行与 PDF 导出使用同一个 Office 引擎；不同引擎的分页可能有差异。
- **模型与解析**：模型需支持工具调用；图片理解和视觉复核需视觉能力。PDF/DOCX 主解析依赖 MinerU 云服务及其 CLI、Token。模型能力检测可能产生少量 API 费用。
- **材料与数据**：桌面版文件、产物和日志保存在本地；完成任务所需内容仍会发送到配置的模型与 MinerU 服务。密钥不得写入公开安装包。

## 3. 核心用户流程

1. **配置服务**：保存 Provider、API Key、模型名及必要的 Base URL；检测工具调用与视觉能力，配置 MinerU Token 和输出目录。
2. **准备材料**：填写目标岗位与可选补充要求；上传 PDF、DOCX、TXT、Markdown、PNG/JPEG/WebP，或在“手动填写”中录入基本信息、教育、经历和技能。两种输入可以同时使用；没有文件也可以开始问答。
3. **选择输出**：选择 t001、t002、t003、t015、t024、t109 六套中文模板之一；指定 DOCX、PDF 或两者、内容优化程度和一页/两页篇幅。
4. **补齐事实并生成**：系统解析材料，对高影响的缺失信息提出结构化问题；用户回答或跳过。界面显示解析、澄清、生成、版式检查和写入产物的进度、错误与取消入口。
5. **检查文件**：在“产物版本”中打开 DOCX/PDF，核对姓名、日期、项目成果、链接、照片和分页，再用于编辑或投递。
6. **修改已有简历**：在新任务中上传原文件，并通过目标岗位和补充要求说明修改方向。后端有内容、样式和模板迁移的语义操作；当前前端没有独立的产物反馈聊天页。

不得编造经历、指标、GitHub Stars/Forks、作品集信息或无法辨认的图片内容；缺少关键事实时应追问或清楚标注。

## 4. 模板与质量边界

- 六套模板共用组件化内容编辑、Word/WPS 测量和排版流程。模板缩略图用于选型，最终分页以实际内容和 Office 引擎为准。
- 图片逐张转录，保留来源与不确定项；原图和机械提取信息可用于按需复核。PDF/DOCX 材料解析依赖 MinerU，视觉检查依赖所选模型的视觉能力。
- 机械检查与视觉检查分别呈现。未运行视觉检查时明确显示“未执行”，不把机械通过等同于完整视觉验收。
- DOCX 是可编辑交付物，PDF 适合预览与投递。应用不提供页面内所见即所得编辑器，最终文件仍需在 Word/WPS 等专业软件中复核。
- 不承诺统计意义上的首稿成功率、跨 Word/WPS 完全一致的分页或任意材料均能自动正确解析。

## 5. 模型与工具边界

桌面版支持 OpenAI Chat Completions、OpenAI Responses、Anthropic 与 OpenAI-compatible 接入；可保存多组 Provider，选择模型、推理深度，并检测工具调用与视觉能力。简历任务只开放当前任务所需的语义工具，不向模型开放通用终端和任意文件操作。

产物、日志和用户材料应按本地任务范围管理。模型与 MinerU 的数据处理遵循所选服务商政策；产品不应宣称“数据从不离开本机”。

## 6. 明确不在当前产品范围

- PPT 生成、通用 DOCX 生成、独立 PDF 转 DOCX：旧工具模式已退役。
- 通用 Chatbot、任意 Skill/插件安装、MCP 市场、通用桌面 Agent、任意命令执行。
- 内置 DOCX/PDF 编辑器或无需 Office 的跨平台排版。
- 独立的产物反馈聊天页、任意模板导入和无条件自动修复所有版式问题。

前端真实实现位于 `src/`，桌面入口为 `src/main.ts`；当前用户功能应以该入口、后端简历流程和 README 为准。`design-mockups/` 中的静态原型仅作历史设计参考，不再是不可变的功能或视觉规范。
