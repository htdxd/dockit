[package]
name = "skill-toolbox"
version = "0.1.0"
description = "Lightweight desktop runtime for artifact-producing skills"
authors = ["Skill Toolbox Contributors"]
edition = "2024"

[lib]
name = "skill_toolbox_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-opener = "2"
tauri-plugin-sql = { version = "2", features = ["sqlite"] }

